apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: docprep-cloud
  labels:
    app.kubernetes.io/name: minio
    app.kubernetes.io/part-of: docprep-cloud
  annotations:
    docprep-cloud.io/placeholder: "Replace with managed S3, MinIO operator, or StatefulSet before production."
spec:
  type: ExternalName
  externalName: minio.example.internal
  ports:
    - name: api
      port: 9000
      targetPort: 9000
