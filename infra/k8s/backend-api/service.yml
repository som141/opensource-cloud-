apiVersion: v1
kind: Service
metadata:
  name: backend-api
  namespace: docprep-cloud
  labels:
    app.kubernetes.io/name: backend-api
    app.kubernetes.io/part-of: docprep-cloud
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend-api
  ports:
    - name: http
      port: 8080
      targetPort: http
