import { PageSection } from '../shared/components/PageSection';

export function ImageDetailPage() {
  return (
    <PageSection
      title="Image detail"
      description="Placeholder for original image, processed image, preview, debug artifacts, and report metadata."
    />
  );
}
