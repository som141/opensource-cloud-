import { PageSection } from '../shared/components/PageSection';

export function ProjectListPage() {
  return (
    <PageSection
      title="Projects"
      description="Placeholder for project list, ownership, member role, default preset, and recent activity."
    />
  );
}
