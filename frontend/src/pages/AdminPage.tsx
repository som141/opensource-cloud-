import { PageSection } from '../shared/components/PageSection';

export function AdminPage() {
  return (
    <PageSection
      title="Admin"
      description="Placeholder for queue length, worker health, error rate, storage usage, and audit signals."
    />
  );
}
