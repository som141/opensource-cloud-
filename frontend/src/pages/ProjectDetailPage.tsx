import { PageSection } from '../shared/components/PageSection';

export function ProjectDetailPage() {
  return (
    <PageSection
      title="Project detail"
      description="Placeholder for project images, recent jobs, project summary, and member access."
    />
  );
}
