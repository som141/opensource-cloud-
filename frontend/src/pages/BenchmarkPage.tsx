import { PageSection } from '../shared/components/PageSection';

export function BenchmarkPage() {
  return (
    <PageSection
      title="Preprocess quality"
      description="Placeholder for optional preprocessing quality checks. This is not an OCR product workflow."
    />
  );
}
