import { PageSection } from '../shared/components/PageSection';

export function UploadPage() {
  return (
    <PageSection
      title="Upload session"
      description="Placeholder for presigned URL upload flow, checksum metadata, and upload completion."
    />
  );
}
